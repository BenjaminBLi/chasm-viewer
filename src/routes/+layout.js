// Render as a single-page application (SPA):
// - `ssr = false` disables server-side rendering so everything runs in the browser.
// - `prerender = true` lets adapter-static generate the fallback shell at build time.
export const prerender = true;
export const ssr = false;
